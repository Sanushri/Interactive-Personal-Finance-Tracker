# Interactive-Personal-Finance-Tracker

Install dependencies:     pip install -r requirements.txt
Run the app:              streamlit run app.py
Run tests:                python3 -m unittest unit_test.py

expenses.json file will be auto created when you run the app and input expense data.
